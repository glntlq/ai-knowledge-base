# 模型成本对比

> 每次对比实验复制本模板为新文件（如 `cost_comparison-2026-05-02.md`），或在下方直接填写本次数据。

---

## 1. 测试条件

| 项目 | 说明 |
|------|------|
| **日期** | YYYY-MM-DD |
| **任务类型** | 例如：知识库条目分析（摘要 + 标签 + JSON 结构化） |
| **输入数据量** | 例如：5 条采集项；约 __ 字/条原文描述；合并 Prompt 约 __ tokens（估算或实测） |
| **输出要求** | 例如：严格 JSON，中文摘要 200–300 字 |
| **温度 / 其他** | temperature=__；max_tokens=__（如有） |
| **计价参考** | 见 `pipeline/model_client.py` 中 CostTracker / 官方价目表；记录当时汇率或备注「按人民币/美元」 |

---

## 2. DeepSeek Chat

| 指标 | 数值 |
|------|------|
| **模型 ID** | `deepseek-chat` |
| **Prompt tokens** | |
| **Completion tokens** | |
| **Total tokens** | |
| **美元成本（估算）** | $ |
| **人民币成本（估算）** | ¥（可选，注明换算方式） |
| **备注** | 例如：流水线日志行、单次请求拆分 |

---

## 3. Qwen Plus

> 若未配置 DashScope / 未跑通本模型，本节可写「未测试」并说明原因。

| 指标 | 数值 |
|------|------|
| **模型 ID** | `qwen-plus`（或实际使用的 Plus 系列名） |
| **Prompt tokens** | |
| **Completion tokens** | |
| **Total tokens** | |
| **美元成本（估算）** | $ |
| **人民币成本（估算）** | ¥（可选） |
| **备注** | |

---

## 4. 结论

| 对比项 | DeepSeek Chat | Qwen Plus |
|--------|----------------|-----------|
| **总 tokens** | | |
| **总成本** | | |
| **单条均价**（总成本 ÷ 条目数） | | |
| **主观质量**（1–5，可选） | | |

**性价比结论（简要）：**

- 在相同测试条件下，**单位成本更低**的是：________  
- **综合质量与成本**更优的是：________  
- **适用建议**：例如批量低成本用 ___；对中文指令遵循要求更高时用 ___。

---

## 附录：原始记录（可选）

粘贴关键日志片段或 API 返回中的 `usage` 字段，便于复核。

```text

```
